import React from "react";

const NotAvailable = () => {
  return (
    <div className="not-available">
      <h1>No movies available for the selected genre.</h1>
    </div>
  );
};

export default NotAvailable;
